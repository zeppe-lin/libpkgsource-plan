# libpkgsource-plan

Native projection adapter extracted from `libpkgsource` 2.1.0.

This import commit preserves the original adapter API and tests as a reviewable
provenance boundary. Subsequent commits establish the independent repository's
first public contract.
